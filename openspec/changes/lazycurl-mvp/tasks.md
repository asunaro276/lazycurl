## 1. プロジェクトセットアップ

- [x] 1.1 Goモジュールを初期化する(`go.mod`、リポジトリ構成、`cmd/lazycurl/main.go`)
- [x] 1.2 依存パッケージを追加する(`charmbracelet/bubbletea`、`charmbracelet/bubbles`、`charmbracelet/lipgloss`)
- [x] 1.3 起動時に`curl --version`を検証する処理を実装する(未検出時はエラー終了、7.70未満は警告)
- [x] 1.4 `~/.config/lazycurl/`のディレクトリ構造を初回起動時に作成する処理を実装する

## 2. `.http`+プラグマ パーサ/シリアライザ

- [x] 2.1 `.http`ファイルを`###`区切りでリクエスト単位にパースする実装
- [x] 2.2 Method/URL/Headers/Bodyを構造体にパースする実装
- [x] 2.3 プラグマコメント(`@insecure`、`@timeout`、`@no-redirect`)をパースする実装、未知プラグマは無視する
- [x] 2.4 リクエスト構造体を`.http`+プラグマ形式へシリアライズする実装
- [x] 2.5 パーサ/シリアライザの往復変換(read→write→read)が一致することを検証するテストを書く

## 3. コレクション管理

- [x] 3.1 `~/.config/lazycurl/collections/`配下の`.http`ファイルを一覧取得する実装
- [x] 3.2 コレクション(ファイル)単位でのリクエスト一覧取得
- [x] 3.3 リクエストの新規作成(空リクエストを`###`ブロックとして追記)
- [x] 3.4 リクエストの複製
- [x] 3.5 リクエストの削除

## 4. environment/変数展開

- [x] 4.1 `env/*.env.json`ファイルの読み込み
- [x] 4.2 コレクションごとのアクティブenvironment切り替え状態の管理
- [x] 4.3 `{{variable}}`展開処理(URL/Headers/Bodyへの適用)
- [x] 4.4 未定義変数を検出し、送信前にエラー表示する処理

## 5. curl実行エンジン

- [x] 5.1 リクエスト構造体からcurl argvを構築する実装(`-X`, `-H`, `--data-binary @tmpfile`)
- [x] 5.2 プラグマからcurlオプションへの変換(`@insecure`→`-k`、`@timeout`→`--max-time`、`@no-redirect`→リダイレクト無効)
- [x] 5.3 `-D`/`-o`/`-w '%{json}'`によるヘッダー・body・タイミング情報の分離取得
- [x] 5.4 `os/exec.CommandContext`によるサブプロセス実行とキャンセル対応
- [x] 5.5 curl終了コードを人間可読なエラーメッセージへ変換するマッピング実装
- [x] 5.6 curl実行部分をインターフェース抽象化し、モックによるユニットテストを可能にする
- [x] 5.7 実行済みargvをシェルクォート文字列として取得する関数(yank用)

## 6. リクエスト編集フォーム(TUI)

- [x] 6.1 Method選択・URL入力コンポーネント
- [x] 6.2 Params/Headersのキー・バリューグリッド編集コンポーネント(行追加・削除・有効/無効)
- [x] 6.3 Authタイプ選択(None/Basic/Bearer)と入力フィールド
- [x] 6.4 Bodyテキストエリアコンポーネント(`bubbles/textarea`)
- [x] 6.5 `ctrl-e`での`$EDITOR`連携(一時ファイル書き出し→エディタ起動→再読み込み)
- [x] 6.6 フォーム状態からリクエスト構造体への変換、保存時の`.http`書き戻し

## 7. TUIシェル(パネル/キーバインド/レスポンス表示)

- [x] 7.1 コレクション一覧パネルの実装
- [x] 7.2 リクエスト一覧パネルの実装
- [x] 7.3 レスポンス表示パネル(status/headers/body/タイミング)の実装
- [x] 7.4 実行履歴パネルの実装
- [x] 7.5 lazygit互換キーバインド(`tab`/`j`/`k`/`enter`/`?`)の実装
- [x] 7.6 メソッド・ステータスコードの色分けバッジ表示
- [x] 7.7 ヘルプ画面(現在のパネルで使えるキー一覧)の実装

## 8. 結合・仕上げ

- [x] 8.1 各パネルを結合したメインアプリケーションループの実装
- [x] 8.2 主要フロー(コレクション作成→リクエスト作成→environment切り替え→送信→履歴確認)の手動確認
- [x] 8.3 README(インストール方法、`curl`依存、`~/.config/lazycurl/`の説明)の作成
